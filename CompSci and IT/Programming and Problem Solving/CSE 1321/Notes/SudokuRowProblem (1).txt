def checkRow(row):
    count = 0
    for char in row:
        if char == " ":
            pass
        else:
            count +=1
    print ("count is "+str(count))
    if count != 9:
        return "String has "+str(count)
    for char in row:
        if char == " ":
            pass
        elif not char.isdigit():
            return "Invalid digit "+str(char)
    a = 0
    b = 0
    c = 0
    d = 0
    e = 0
    f = 0
    g = 0
    h = 0
    i = 0
    for char in row:
        if char == " ":
            pass
        elif char == '1':
            a +=1
            if a > 1:
                return "1 is repeated"
        elif char == '2':
            b += 1
            if b > 1:
                return "2 is repeated"
        elif char == '3':
            c +=1
            if c > 1:
                return "3 is repeated"
        elif char == '4':
            d += 1
            if d > 1:
                print("d is "+str(d))
                return "4 is repeated"
        elif char == '5':
            e +=1
            if e > 1:
                return "5 is repeated"
        elif char == '6':
            f += 1
            if f > 1:
                return "6 is repeated"
        elif char == '7':
            g +=1
            if g > 1:
                return "7 is repeated"
        elif char == '8':
            h += 1
            if h > 1:
                return "8 is repeated"
        elif char == '9':
            i +=1
            if i > 1:
                return "9 is repeated"
        else:
            return ("row is good")


row = input("enter your characters ")
ans = checkRow(row)
print("ans is "+ ans)
if ans == False:
    print("invalid digit")